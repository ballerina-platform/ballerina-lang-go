module $anon.. v 0.0.0;
import ballerina.io v 0.0.0;
import ballerina.io v 0.0.0;
$desugar$0  object { public function attach(service object, nil) returns nil; public function detach(service object) returns nil|error; public function gracefulStop() returns nil|error; public function immediateStop() returns nil|error; public function init(string) returns nil; public function start() returns nil|error; private string tag };
$desugar$1  object { public function attach(service object, nil) returns nil; public function detach(service object) returns nil|error; public function gracefulStop() returns nil|error; public function immediateStop() returns nil|error; public function init(string) returns nil; public function start() returns nil|error; private string tag };
$moduleListeners  [object { public function attach(never, [string...]) returns nil|error; public function detach(never) returns nil|error; public function gracefulStop() returns nil|error; public function immediateStop() returns nil|error; public function start() returns nil|error }|object { public function attach(never, string) returns nil|error; public function detach(never) returns nil|error; public function gracefulStop() returns nil|error; public function immediateStop() returns nil|error; public function start() returns nil|error }|object { public function attach(never, nil) returns nil|error; public function detach(never) returns nil|error; public function gracefulStop() returns nil|error; public function immediateStop() returns nil|error; public function start() returns nil|error }...];
l  object { public function attach(service object, nil) returns nil; public function detach(service object) returns nil|error; public function gracefulStop() returns nil|error; public function immediateStop() returns nil|error; public function init(string) returns nil; public function start() returns nil|error; private string tag };
class TaggedListener {
  tag string

  attach(service object,nil) -> nil{
    bb0 {
      _ = svc;
      _ = attachPoint;
      %6 = ConstantLoad attached: 
      %8 = ConstantLoad tag
      %7 = self[%8];
      %9 = println(%6,%7) -> bb1;
    }
    bb1 {
      return;
    }
  }

  detach(service object) -> nil|error{
    bb0 {
      _ = svc;
      return;
    }
  }

  gracefulStop() -> nil|error{
    bb0 {
      return;
    }
  }

  immediateStop() -> nil|error{
    bb0 {
      return;
    }
  }

  init(string) -> nil{
    bb0 {
      %3 = ConstantLoad tag
      self[%3] = tag;
      return;
    }
  }

  start() -> nil|error{
    bb0 {
      return;
    }
  }
}
class $service$0 {

  init() -> nil{
    bb0 {
      return;
    }
  }
}
makeListener(string) -> object { public function attach(service object, nil) returns nil; public function detach(service object) returns nil|error; public function gracefulStop() returns nil|error; public function immediateStop() returns nil|error; public function init(string) returns nil; public function start() returns nil|error; private string tag }{
  bb0 {
    %2 = newObject $anon/.:TaggedListener
    %3 = init(%2,tag) -> bb1;
  }
  bb1 {
    %5 = %3 is nil
    %5 ? bb2 : bb3;
  }
  bb2 {
    %4 = %2;
    GOTO bb4;
  }
  bb3 {
    %4 = %3;
    GOTO bb4;
  }
  bb4 {
    %0 = %4;
    return;
  }
}
main() -> nil{
  bb0 {
    return;
  }
}
$default$0(service object) -> nil{
  bb0 {
    %2 = ConstantLoad <nil>
    %0 = %2;
    return;
  }
}
