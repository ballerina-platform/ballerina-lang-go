Local repository direct and transitive: both the direct dependency (`mockorg/outer`) and its transitive dependency (`mockorg/inner`) are pinned to `repository = "local"` in the consumer's `Ballerina.toml`. Both packages exist in both local and central with distinguishable content. The test verifies that the local copy wins for both the direct dep and the transitive dep — output contains `local-outer says local-inner`.
