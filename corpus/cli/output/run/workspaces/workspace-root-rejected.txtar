-- stdout --
-- stderr --
ballerina: cannot run a workspace project directly. Use 'bal run <package-path>' to run a specific package within the workspace

USAGE:
    run [<source-file.bal> | <package-dir> | .]
-- exitcode --
1
